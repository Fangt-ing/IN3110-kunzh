cmd: which bash
outputs: /usr/bin/bash

this code help to exit a source cmd
cmd: exec bash

$sourcefile/*.$file_type (file_type needs to be indicated mannually)

break/ continue

create dir with time appended.
mkdir folder-$(date +"%Y-%h-%d_%H:%M")

TASK2
$(date +%s)

date +"%a %b %d %T %Z %Y"

This converts all to seconds.

tail -1 file.txt
to check the last line of a file.

track start "Task 1"

echo "i am a cat" | cut -d' ' -f3-